# @ophir/escrow

Anchor program for Solana USDC escrow.

This package will contain the Anchor-based escrow smart contract that holds USDC during active negotiations and releases funds based on SLA compliance.

## Setup

Will be initialized with `anchor init` in a later step.
