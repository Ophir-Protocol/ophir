{
  "name": "@ophirai/protocol",
  "version": "0.3.0",
  "description": "Core protocol definitions for the Ophir Agent Negotiation Protocol — types, schemas, state machine, SLA metrics, and error codes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Ophir-Protocol/ophir",
    "directory": "packages/protocol"
  },
  "keywords": ["ai-agents", "negotiation", "protocol", "sla", "agent-to-agent", "json-rpc", "ophir"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "files": ["dist", "README.md"],
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
