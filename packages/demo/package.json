{
  "name": "@ophirai/demo",
  "version": "0.2.0",
  "description": "Ophir protocol demos — self-negotiating agents, full lifecycle showcase",
  "type": "module",
  "private": true,
  "scripts": {
    "demo": "tsx src/self-negotiating.ts",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ophirai/protocol": "*",
    "@ophirai/sdk": "*",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "tsx": "^4.0.0"
  }
}
